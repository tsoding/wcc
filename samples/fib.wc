// NOTE: To test this sample just do `make test_fib` in the root of the project.
// For more information do `make help`.

func fib_u32(n: u32): u32 {
    local a: u32 = 0;
    local b: u32 = 1;
    local c: u32 = 0;
    while (n > 0) {
        c = a + b;
        a = b;
        b = c;
        n -= 1;
    }
    a;
}

func fib_u64(n: u64): u64 {
    local a: u64 = 0;
    local b: u64 = 1;
    local c: u64 = 0;
    while (n > 0) {
        c = a + b;
        a = b;
        b = c;
        n -= 1;
    }
    a;
}

// TODO: add fib_u8 sample