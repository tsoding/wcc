// NOTE: To test this sample just do `make test_add` in the root of the project.
// For more information do `make help`.

func add_u8(a: u8, b: u8): u8 {
    a + b;
}

func add_u32(a: u32, b: u32): u32 {
    a + b;
}

func add_u64(a: u64, b: u64): u64 {
    a + b;
}
